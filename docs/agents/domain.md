# Domain documentation

Soloe currently uses a single domain context.

Before changing behavior, read:

- `CONTEXT.md`
- relevant records under `docs/adr/`, when present

Use the glossary's exact vocabulary in tests, code, and documentation. If a
change conflicts with an architecture decision record, surface the conflict
instead of silently overriding it.
